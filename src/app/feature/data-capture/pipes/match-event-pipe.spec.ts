import { MatchEventPipe } from './match-event-pipe';

describe('MatchEventPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchEventPipe();
    expect(pipe).toBeTruthy();
  });
});
